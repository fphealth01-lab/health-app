import { PagePlaceholder } from '@/components/app/page-placeholder'

export const metadata = { title: 'AI Coach' }

export default function CoachPage() {
  return (
    <PagePlaceholder
      title="AI Coach"
      description="Ask anything about your protocol. Citation-backed, memory across sessions on Premium."
    />
  )
}
